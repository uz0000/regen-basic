# Pinned: determinism holds within a fixed dependency set, not across numpy
# major versions — this was tested, not assumed, on an earlier version of
# this codebase (a newer numpy/scikit-learn shifted results enough to flip a
# borderline test there). Kept pinned here on the same basis.
numpy==1.26.4
pandas==2.1.4
scipy==1.11.4

# Dev / test
pytest
